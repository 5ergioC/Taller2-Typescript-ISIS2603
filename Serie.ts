export class Serie{

    constructor(public id: number, public nombre: string, public plataforma: string, public temporadas: number, public sinapsis: string, public link: string, public foto: string)
    {

    }

}